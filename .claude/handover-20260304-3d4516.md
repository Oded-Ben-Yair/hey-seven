# Hey Seven Session Handover

**Session**: hey-seven-session-20260304-3d4516
**Date**: 2026-03-04
**Round**: R88
**Blueprint Phase**: Phase B: Behavioral Uplift (Start)
**Commit**: d242b6a

---

## 30-Dimension Scorecard

### Technical (D1-D10): 9.63/10 — DONE
Last evaluated: R75 (4-model consensus). Infrastructure complete.

### Behavioral (B1-B10): 5.2/10 — Target 8.0
R87 baseline (first formal 2-model judge panel):

| Dim | Name | GPT-5.2 | Grok 4 | Consensus | Target | Gap |
|-----|------|---------|--------|-----------|--------|-----|
| B1 | Sarcasm & Tone | 7 | 10 | 8.5 | 8.0 | MET |
| B2 | Implicit Signals | 5 | 7 | 6.0 | 8.0 | -2.0 |
| B3 | Engagement | 3 | 4 | 3.5 | 8.0 | -4.5 |
| B4 | Agentic/Proactive | 3.5 | 6 | 4.8 | 8.0 | -3.2 |
| B5 | Emotional Nuance | 5.5 | 7 | 6.3 | 8.0 | -1.7 |
| B6 | Tone Calibration | 2 | 7 | 4.5 | 8.0 | -3.5 |
| B7 | Multi-turn Coherence | 5.5 | 6.5 | 6.0 | 8.0 | -2.0 |
| B8 | Cultural/Multilingual | 5 | 6 | 5.5 | 8.0 | -2.5 |
| B9 | Safety/Compliance | 4 | 4.5 | 4.3 | 9.0 | -4.7 |
| B10 | Overall Quality | 2.5 | 4 | 3.3 | 8.0 | -4.7 |

**Biggest gaps**: B3 Engagement (-4.5), B9 Safety (-4.7), B10 Overall (-4.7), B6 Tone (-3.5)
**Met**: B1 Sarcasm (8.5)
**Note**: B9/B10 scores dragged down by fallback rate (16-23%). Fallback is a RAG/validation issue, not a behavioral code issue.

### Profiling (P1-P10): 5.5/10 — Target 7.0
| Dim | Name | Estimate | Target | Gap |
|-----|------|----------|--------|-----|
| P1 | Name capture | 8.0 | 8.0 | MET |
| P2 | Active probing | 5.0 | 7.0 | -2.0 |
| P3 | Preference learning | 4.0 | 7.0 | -3.0 |
| P4 | Context retention | 5.0 | 7.0 | -2.0 |

### Host Triangle (H1-H10): NOT MEASURED — Target 6.0
Pre-Phase D. Not yet evaluated.

### Safety: 95%+ — Target 100%
Crisis de-escalation verified working (R88). Spanish crisis detection working (R85).
Gap: Repetitive crisis responses (same text T2=T3), placeholder leaks ([PHONE]).

---

## This Session's Work

### Deliverables Completed
1. **R87 baseline scored** — first formal 2-model judge panel (GPT-5.2 + Grok 4)
2. **Crisis de-escalation** — compliance_gate.py: broader safe confirmations + agrees-to-seek-help exit
3. **Post-gen enforcement** — nodes.py: "As an AI" stripper, deflection stripper, [NAME]->Seven
4. **Cross-domain bridges** — _base.py: property-specific venue names (Wolf Den, Tuscany, Mandara, MJ's, Elemis)
5. **Incentive gate 50%->25%** — incentives.py + test updates
6. **Greeting closers** — nodes.py: cross-domain suggestions vs thin "Anything else?"
7. **Sarcasm patterns +4** — sentiment.py: chatbot skepticism, dripping sarcasm
8. **Persona opener strip** — persona.py: "I appreciate you asking!" removed
9. **Eval v2 re-run** — 20/20 scenarios, 60/60 turns, 0 errors

### Tests
- Total: 3298 passed, 22 pre-existing failures (NOT from R88)
- New/modified: test_incentives.py (3 threshold tests), test_nodes.py (1 assertion update)
- Pre-existing failures: vaderSentiment missing (11), hypothesis missing (5), redis mock (6)

### Key Decisions
- R87 baseline consensus methodology: median of GPT-5.2 and Grok 4 per-scenario scores
- Crisis exit: `_agrees_to_seek_help` path allows transition WITHOUT requiring a property question
- Incentive gate 25% chosen because 3-turn conversations rarely reach 50% profile completeness

### Learnings
- **vaderSentiment not installed in WSL venv** — all sentiment falls to "neutral". This degrades sarcasm/frustration detection in eval. Fix: `pip install vaderSentiment`
- **hypothesis not installed** — 5 property-based test files can't run. Fix: `pip install hypothesis`
- **Fallback rate is LLM-nondeterministic** — same code produces 16% or 23% fallback rate across runs. Root cause is RAG retrieval + validation, not behavioral code.
- **Judge panel calibration**: Grok inflates ~2 points above GPT-5.2 consistently. Use GPT-5.2 as ground truth, Grok for second opinion.
- **Cross-domain bridges need venue data** — generic "the spa is nice" scored B4=3-4. Specific "Mandara Spa's Balinese-inspired relaxation lounge" scored higher.

---

## Blueprint Next Session: R89

### Phase: Phase B: Behavioral Uplift (Continue)

### P0: Environment Fix
- `pip install vaderSentiment hypothesis` — fixes 22 test failures AND enables proper sentiment detection in eval

### P1: Investigate Fallback Rate (23%)
- Root cause analysis: why does `retrieve_node` return empty for "What restaurants do you have?"
- Check ChromaDB ingestion: `data/chroma/` has 20MB — is it fully populated?
- Consider: skip validation when RAG returns good context (validator may be too strict)

### P2: Run Scored Judge Panel on R88 Eval Output
- R88 eval output already exists in `tests/evaluation/v2-results/`
- Score with GPT-5.2 + Grok 4 to measure delta from R87 baseline
- Expected: crisis scenarios should score higher (de-escalation working)

### P3: B3 Engagement Improvement (Gap: -4.5)
- Overwhelm detection: when guest says "just pick one", collapse to SINGLE recommendation
- Follow-up questions: every response should end with a natural question
- Dead-end prevention: never end with "Anything else?" without a specific suggestion

### P4: B6 Tone Calibration (Gap: -3.5)
- [NAME] placeholder still leaking from LLM (post-gen catches it, but prevention is better)
- Consistent concierge register across all turns (no "As an AI" at any point)
- Warmth through substance, not exclamation marks

### Expected Impact
- B-avg: 5.2 -> 6.5 (fallback rate reduction + engagement + tone)
- P-avg: 5.5 -> 6.0 (environment fix enables sentiment -> better profiling)
- Safety: 95% -> 98% (crisis de-escalation already landed)

---

## Optimal Execution Strategy

### Recommended Mode
SOLO — sequential steps, each depends on prior.

### Key Tools for Next Session

#### MCP Tools
- `azure_chat` (GPT-5.2) — judge panel scoring
- `grok_reason` (Grok 4) — judge panel second opinion
- `gemini-query` — if needing to debug Gemini Flash behavior

#### Eval
- `tests/evaluation/v2/cli.py run` — 20-scenario behavioral eval
- Judge panel: GPT-5.2 + Grok 4 (proven reliable)
- Always clear old results before re-running: `rm tests/evaluation/v2-results/*.json`

### Verification Plan
1. `pip install vaderSentiment hypothesis` -> re-run tests -> 22 failures fixed
2. `pytest tests/ -x -q` -> all pass (excluding pre-existing redis/state issues)
3. Eval v2 completion rate > 95%
4. Judge panel scores show improvement vs R87 baseline (consensus 5.2)

---

## Quick Resume Command

```
/go
```

This handover + MEMORY.md provides everything /go needs.
